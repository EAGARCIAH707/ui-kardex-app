const host = 'http://3.16.120.58:80/cidenet-kardex/v1';
export const environment = {
  production: true,
  login_endpoint: `${host}/login`,
  get_kardex: `${host}/kardex`,
  create_product: `${host}/product`,
  add_in: `${host}/in`,
  add_out: `${host}/out`
};

